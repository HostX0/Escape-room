NODE_ENV=development
PORT=5000
DATABASE_URL=postgresql://postgres:REPLACE_LOCALLY@localhost:5432/escape_room_test
JWT_SECRET=
ADMIN_JWT_SECRET=
BCRYPT_ROUNDS=12
PG_POOL_MAX=2
CORS_ORIGIN=http://localhost:5173
# Optional WhatsApp verification; no default credentials or OTP logging.
ULTRAMSG_INSTANCE_ID=
ULTRAMSG_TOKEN=
# Opt-in test seeding only; never use against another project's database.
TEST_DATABASE_CONFIRM=
TEST_SEED_PASSWORD=
